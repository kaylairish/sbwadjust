% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sbw_estimate.R
\name{sbw_estimate}
\alias{sbw_estimate}
\alias{sbw_estimate.sbw_fit}
\title{Estimate a treatment effect from a fitted \code{sbw_fit} object}
\usage{
sbw_estimate(object, outcome, estimand, ...)

\method{sbw_estimate}{sbw_fit}(
  object,
  outcome,
  estimand,
  probs = 0.5,
  horizon = NULL,
  B = 1500,
  alpha = 0.05,
  ci_method = c("wald", "percentile"),
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{An \code{sbw_fit} from \code{\link[=sbw_weights]{sbw_weights()}}.}

\item{outcome}{A one-sided-response formula naming the outcome, e.g.
\code{Y ~ 1} (or \code{Surv(time, status) ~ 1} for \code{estimand = "survival_ratio"},
which requires the \code{survival} package to be attached so \code{Surv()}
resolves in the formula's environment).}

\item{estimand}{One of \code{"ATE"}, \code{"RR"} (relative risk, i.e. ratio of
weighted arm means), \code{"survival_ratio"}, \code{"mann_whitney"},
\code{"quantile_diff"}, or \code{"quantile_ratio"}. No user-supplied functional is
accepted; an uncovered estimand means: take \code{weights(object)} and run
(and bootstrap) your own estimator.}

\item{...}{Passed to methods.}

\item{probs}{Vector of probabilities in (0, 1), for
\verb{estimand \\\%in\\\% c("quantile_diff", "quantile_ratio")}. Default \code{0.5}
(the median).}

\item{horizon}{Time \code{t0} at which to evaluate the survival ratio; required
for \code{estimand = "survival_ratio"}.}

\item{B}{Number of bootstrap replicates.}

\item{alpha}{Significance level for the confidence interval.}

\item{ci_method}{Either \code{"wald"} (default) or \code{"percentile"}.}

\item{seed}{Optional seed set at the start of the bootstrap.}
}
\value{
An object of class \code{sbw_estimate}.
}
\description{
Estimate a treatment effect from a fitted \code{sbw_fit} object
}
