% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/km_rmst.R
\name{boot_km_rmst}
\alias{boot_km_rmst}
\title{Bootstrap CI for a weighted-KM RMST difference (SBW or IPW)}
\usage{
boot_km_rmst(
  time,
  status,
  A,
  X_subset,
  t_star,
  B = 1500,
  alpha = 0.05,
  weight_type = c("SBW", "IPW"),
  ci_method = c("wald", "percentile"),
  ipw_use_glm = TRUE,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{time}{Event/censoring time.}

\item{status}{Event indicator (1 = event, 0 = censored).}

\item{A}{Treatment indicator (0/1).}

\item{X_subset}{Covariate data frame for the full study sample, used to
fit the SBW/IPW weights.}

\item{t_star}{Horizon at which to compute the restricted mean survival time.}

\item{B}{Number of bootstrap replicates.}

\item{alpha}{Significance level for the confidence interval (default 0.05).}

\item{weight_type}{Either \code{"SBW"} (\code{\link[=get_sbws_for_study]{get_sbws_for_study()}}) or \code{"IPW"}
(\code{\link[=get_ipws_for_study]{get_ipws_for_study()}}).}

\item{ci_method}{Either \code{"wald"} (default) or \code{"percentile"}.}

\item{ipw_use_glm}{Passed through to \code{\link[=get_ipws_for_study]{get_ipws_for_study()}} when
\code{weight_type = "IPW"}.}

\item{seed}{Optional seed set at the start of the bootstrap.}

\item{verbose}{Currently unused; reserved for future diagnostic output.}
}
\value{
A list with \code{MC_fail}, \code{est} (the RMST difference), \code{se}, \code{ci},
\code{boot_fail_rate}, \code{boot_n_finite_reps}, and SBW clipping diagnostics
(\code{sbw_n_clipped_full}, \code{sbw_max_abs_clipped_full},
\code{sbw_n_clipped_boot_mean}, \code{sbw_n_clipped_boot_max},
\code{sbw_max_abs_clipped_boot_max}; all \code{NA} when \code{weight_type = "IPW"}).
}
\description{
Bootstraps the standard error of \code{\link[=weighted_km_rmst_diff]{weighted_km_rmst_diff()}}'s difference,
then builds either a Wald CI or a percentile CI (both on the natural,
additive scale -- RMST difference needs no log transform, unlike the
survival-ratio estimand). If the point estimate or bootstrap SE come out
non-finite, falls back to the unadjusted RMST difference and sets
\code{MC_fail = TRUE}; if even the unadjusted difference is undefined (a
double-degenerate case with no valid fallback), throws an error rather
than returning a nonsense finite estimate. Structurally identical to
\code{\link[=boot_km_ratio]{boot_km_ratio()}} -- see its docs for the shared fallback logic.
}
