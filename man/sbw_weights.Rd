% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sbw_weights.R
\name{sbw_weights}
\alias{sbw_weights}
\title{Fit stable balancing weights for a two-arm study (design stage)}
\usage{
sbw_weights(balance, data, treatment)
}
\arguments{
\item{balance}{One-sided formula naming the covariates to balance, e.g.
\code{~ age + sex + bmi + region}.}

\item{data}{Data frame containing the balance covariates and treatment.}

\item{treatment}{Treatment assignment: either a bare column name in
\code{data} (unquoted, as in \code{treatment = arm}) or a vector. Accepts numeric
0/1, logical, or a two-level factor/character vector.}
}
\value{
An object of class \code{sbw_fit} with elements \code{weights}, \code{n_clipped},
\code{max_abs_clipped} (see \code{\link[=get_sbws_for_study]{get_sbws_for_study()}}), \code{balance} (the formula),
\code{treatment_name}, \code{treatment_levels}, \code{treatment} (recoded 0/1),
\code{X} (the balance design matrix), \code{data}, \code{n}, and \code{call}.
}
\description{
Formula-based front end to \code{\link[=get_sbws_for_study]{get_sbws_for_study()}}: balances the covariates
named in \code{balance} (factors dummy-coded automatically) to the pooled-arm
mean, exactly (imbalance tolerance fixed at zero), matching the paper's
SBW specification. Intended to be called before unblinding, using only
baseline covariates and the randomization assignment.
}
\examples{
set.seed(1)
n = 100
trial_baseline = data.frame(
  age = rnorm(n, 50, 10),
  region = sample(c("N", "S"), n, replace = TRUE),
  arm = rbinom(n, 1, 0.5)
)
sbw = sbw_weights(~ age + region, data = trial_baseline, treatment = arm)
sbw
summary(sbw)
}
