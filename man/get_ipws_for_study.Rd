% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ipw.R
\name{get_ipws_for_study}
\alias{get_ipws_for_study}
\title{Stabilized inverse-probability-of-treatment weights for KM estimation}
\usage{
get_ipws_for_study(
  X_subset,
  A,
  use_glm = TRUE,
  p_known = 0.5,
  pi = NULL,
  trim = c(0.01, 0.99),
  return_ps = FALSE
)
}
\arguments{
\item{X_subset}{Covariate data frame for the full study sample.}

\item{A}{Treatment indicator (0/1).}

\item{use_glm}{If \code{TRUE} (default), estimate the propensity score via
\code{stats::glm(A ~ ., family = binomial())}. If \code{FALSE}, use the fixed
\code{p_known} for every unit (e.g. for a known randomization probability).}

\item{p_known}{Propensity score used for every unit when \code{use_glm = FALSE}.}

\item{pi}{Marginal treatment probability used for stabilization; if \code{NULL}
(default), uses \code{mean(A)}.}

\item{trim}{Currently unused; reserved for propensity-score trimming.}

\item{return_ps}{Currently unused; reserved for returning the fitted
propensity scores alongside the weights.}
}
\value{
Numeric vector of stabilized IPTW weights, one per row of
\code{X_subset}.
}
\description{
\code{w = pi / ps} for treated units, \code{(1 - pi) / (1 - ps)} for control units,
where \code{ps} is the estimated propensity score and \code{pi} is the marginal
treatment probability used for stabilization.
}
